services:
  mcp-material:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENV=dev
      - HOST=0.0.0.0
      - PORT=8080
      - DATA_ROOT=/app/data
      - RESOURCES_ROOT=/app/resources
    volumes:
      - ./resources:/app/resources:ro
      - ./data:/app/data

